export enum OperationStatus {
    Running = 'running',
    Success = 'success',
    Failed = 'failed',
    Conflict = 'conflict',
}

export enum OperationNoticeActionKind {
    ShowOutput = 'showOutput',
    Retry = 'retry',
}
